/// <reference types="node" />
import { AxiosInstance, AxiosProgressEvent } from "axios";
export default class FileApi {
    private multipartConfig;
    private serializer;
    private readonly baseAppId;
    private readonly sellerAppId;
    private readonly sellerUserId;
    private axios;
    constructor(axios: AxiosInstance, baseAppId: number, sellerUserId: string, sellerAppId: number);
    save(file: Buffer, filename: string, isPublic: boolean, onProgress?: ((event: AxiosProgressEvent) => {})): Promise<any>;
    delete(key: string): Promise<void>;
    getSignedUrl(key: string): Promise<any>;
}
export declare class Blog {
    key?: string | undefined;
    url?: string | undefined;
    constructor(key?: string | undefined, url?: string | undefined);
}
